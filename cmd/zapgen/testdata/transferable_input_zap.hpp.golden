// Code generated by zapgen; DO NOT EDIT.
// source: basetx.zap
// SPDX-License-Identifier: BSD-3-Clause-Eco

#pragma once

#include <cstdint>
#include <expected>
#include <span>
#include <vector>

#include <zap/zap.hpp>

namespace xvm {

class TransferableInput;

// Field byte offsets for TransferableInput, and the size of its fixed section.
inline constexpr std::int64_t kTransferableInputOffsetOff = 0;
inline constexpr std::int64_t kTransferableInputSize = 4;

// TransferableInput is a zero-copy view into a ZAP-encoded TransferableInput message. It borrows the
// bytes it was built over, which must outlive it, and copies no field.
class TransferableInput {
  public:
    TransferableInput() = default;
    explicit TransferableInput(zap::Object o) : o_(o) {}

    bool is_null() const { return o_.is_null(); }
    zap::Object object() const { return o_; }

    std::uint32_t Offset() const { return o_.u32(kTransferableInputOffsetOff); }

  private:
    zap::Object o_;
};

// WrapTransferableInput parses b and returns a typed view over it, or says why b is not a
// message. The view borrows b.
inline std::expected<TransferableInput, zap::Error> WrapTransferableInput(std::span<const std::uint8_t> b) {
    auto m = zap::Message::parse(b);
    if (!m) return std::unexpected(m.error());
    return TransferableInput(m->root());
}

// TransferableInputInput collects the field values for NewTransferableInput. A borrowed field is a view:
// it must outlive the call, not the buffer the call returns.
struct TransferableInputInput {
    std::uint32_t Offset{};
};

// NewTransferableInput writes a ZAP-encoded TransferableInput message into a fresh buffer and returns it.
inline std::vector<std::uint8_t> NewTransferableInput(const TransferableInputInput& in) {
    zap::Builder b(256, zap::kVersion1);
    auto ob = b.start_object(kTransferableInputSize);
    ob.set_u32(kTransferableInputOffsetOff, in.Offset);
    ob.finish_as_root();
    return b.finish();
}

}  // namespace xvm
