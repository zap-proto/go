// Code generated by zapgen; DO NOT EDIT.
// source: echo.zap
// SPDX-License-Identifier: BSD-3-Clause-Eco

#pragma once

#include <array>
#include <cstdint>
#include <cstring>
#include <expected>
#include <span>
#include <vector>

#include <zap/zap.hpp>

namespace echo {

class Ping;

// Field byte offsets for Ping, and the size of its fixed section.
inline constexpr std::int64_t kPingSeqOff = 0;
inline constexpr std::int64_t kPingSize = 8;

// Ping is a zero-copy view into a ZAP-encoded Ping message. It borrows the
// bytes it was built over, which must outlive it, and copies no field.
class Ping {
  public:
    Ping() = default;
    explicit Ping(zap::Object o) : o_(o) {}

    bool is_null() const { return o_.is_null(); }
    zap::Object object() const { return o_; }

    std::uint64_t Seq() const { return o_.u64(kPingSeqOff); }

  private:
    zap::Object o_;
};

// WrapPing parses b and returns a typed view over it, or says why b is not a
// message. The view borrows b.
inline std::expected<Ping, zap::Error> WrapPing(std::span<const std::uint8_t> b) {
    auto m = zap::Message::parse(b);
    if (!m) return std::unexpected(m.error());
    return Ping(m->root());
}

// The value records, declared before they are defined.
struct PingInput;

// PingInput collects the field values for NewPing. A borrowed field is a view:
// it must outlive the call, not the buffer the call returns.
struct PingInput {
    std::uint64_t Seq{};
};

// The write side, declared before it is defined so a struct may point at
// one that comes later in the schema.
inline std::vector<std::uint8_t> NewPing(const PingInput& in);
inline std::int64_t AppendPing(zap::Builder& b, const PingInput& in);
inline std::array<std::uint8_t, static_cast<std::size_t>(kPingSize)> EncodePing(const PingInput& in);

// EncodePing lays one Ping payload out as an inline list element:
// the fixed section alone, unaligned, exactly as wide as the stride.
inline std::array<std::uint8_t, static_cast<std::size_t>(kPingSize)> EncodePing(const PingInput& in) {
    std::array<std::uint8_t, static_cast<std::size_t>(kPingSize)> e{};
    zap::store_u64(e.data() + kPingSeqOff, in.Seq);
    return e;
}

// AppendPing writes one Ping object into b — every payload it points at first,
// then the object itself — and returns the object's offset.
inline std::int64_t AppendPing(zap::Builder& b, const PingInput& in) {
    auto ob = b.start_object(kPingSize);
    ob.set_u64(kPingSeqOff, in.Seq);
    return ob.finish();
}

// NewPing writes a ZAP-encoded Ping message into a fresh buffer and returns it.
inline std::vector<std::uint8_t> NewPing(const PingInput& in) {
    zap::Builder b(256, zap::kVersion2);
    auto ob = b.start_object(kPingSize);
    ob.set_u64(kPingSeqOff, in.Seq);
    ob.finish_as_root();
    return b.finish();
}

}  // namespace echo
