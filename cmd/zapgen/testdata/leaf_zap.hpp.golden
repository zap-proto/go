// Code generated by zapgen; DO NOT EDIT.
// source: packed.zap
// SPDX-License-Identifier: BSD-3-Clause-Eco

#pragma once

#include <array>
#include <cstdint>
#include <cstring>
#include <expected>
#include <span>
#include <vector>

#include <zap/zap.hpp>

namespace packed {

class Leaf;

// Field byte offsets for Leaf, and the size of its fixed section.
inline constexpr std::int64_t kLeafWeightOff = 0;
inline constexpr std::int64_t kLeafIndexOff = 8;
inline constexpr std::int64_t kLeafSize = 12;

// Leaf is a zero-copy view into a ZAP-encoded Leaf message. It borrows the
// bytes it was built over, which must outlive it, and copies no field.
class Leaf {
  public:
    Leaf() = default;
    explicit Leaf(zap::Object o) : o_(o) {}

    bool is_null() const { return o_.is_null(); }
    zap::Object object() const { return o_; }

    std::uint64_t Weight() const { return o_.u64(kLeafWeightOff); }
    std::uint32_t Index() const { return o_.u32(kLeafIndexOff); }

  private:
    zap::Object o_;
};

// WrapLeaf parses b and returns a typed view over it, or says why b is not a
// message. The view borrows b.
inline std::expected<Leaf, zap::Error> WrapLeaf(std::span<const std::uint8_t> b) {
    auto m = zap::Message::parse(b);
    if (!m) return std::unexpected(m.error());
    return Leaf(m->root());
}

// The value records, declared before they are defined.
struct LeafInput;

// LeafInput collects the field values for NewLeaf. A borrowed field is a view:
// it must outlive the call, not the buffer the call returns.
struct LeafInput {
    std::uint64_t Weight{};
    std::uint32_t Index{};
};

// The write side, declared before it is defined so a struct may point at
// one that comes later in the schema.
inline std::vector<std::uint8_t> NewLeaf(const LeafInput& in);
inline std::int64_t AppendLeaf(zap::Builder& b, const LeafInput& in);
inline std::array<std::uint8_t, static_cast<std::size_t>(kLeafSize)> EncodeLeaf(const LeafInput& in);

// EncodeLeaf lays one Leaf payload out as an inline list element:
// the fixed section alone, unaligned, exactly as wide as the stride.
inline std::array<std::uint8_t, static_cast<std::size_t>(kLeafSize)> EncodeLeaf(const LeafInput& in) {
    std::array<std::uint8_t, static_cast<std::size_t>(kLeafSize)> e{};
    zap::store_u64(e.data() + kLeafWeightOff, in.Weight);
    zap::store_u32(e.data() + kLeafIndexOff, in.Index);
    return e;
}

// AppendLeaf writes one Leaf object into b — every payload it points at first,
// then the object itself — and returns the object's offset.
inline std::int64_t AppendLeaf(zap::Builder& b, const LeafInput& in) {
    auto ob = b.start_object(kLeafSize);
    ob.set_u64(kLeafWeightOff, in.Weight);
    ob.set_u32(kLeafIndexOff, in.Index);
    return ob.finish();
}

// NewLeaf writes a ZAP-encoded Leaf message into a fresh buffer and returns it.
inline std::vector<std::uint8_t> NewLeaf(const LeafInput& in) {
    zap::Builder b(256, zap::kVersion2);
    auto ob = b.start_object(kLeafSize);
    ob.set_u64(kLeafWeightOff, in.Weight);
    ob.set_u32(kLeafIndexOff, in.Index);
    ob.finish_as_root();
    return b.finish();
}

}  // namespace packed
