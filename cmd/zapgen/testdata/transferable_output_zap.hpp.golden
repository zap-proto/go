// Code generated by zapgen; DO NOT EDIT.
// source: basetx.zap
// SPDX-License-Identifier: BSD-3-Clause-Eco

#pragma once

#include <cstdint>
#include <expected>
#include <span>
#include <vector>

#include <zap/zap.hpp>

namespace xvm {

class TransferableOutput;
class TransferableOutputList;

// Field byte offsets for TransferableOutput, and the size of its fixed section.
inline constexpr std::int64_t kTransferableOutputOffsetOff = 0;
inline constexpr std::int64_t kTransferableOutputSize = 4;

// TransferableOutput is a zero-copy view into a ZAP-encoded TransferableOutput message. It borrows the
// bytes it was built over, which must outlive it, and copies no field.
class TransferableOutput {
  public:
    TransferableOutput() = default;
    explicit TransferableOutput(zap::Object o) : o_(o) {}

    bool is_null() const { return o_.is_null(); }
    zap::Object object() const { return o_; }

    std::uint32_t Offset() const { return o_.u32(kTransferableOutputOffsetOff); }

    // The kTransferableOutputSize bytes this TransferableOutput occupies where it lies.
    std::span<const std::uint8_t> Record() const { return o_.bytes_fixed(0, kTransferableOutputSize); }

  private:
    zap::Object o_;
};

// WrapTransferableOutput parses b and returns a typed view over it, or says why b is not a
// message. The view borrows b.
inline std::expected<TransferableOutput, zap::Error> WrapTransferableOutput(std::span<const std::uint8_t> b) {
    auto m = zap::Message::parse(b);
    if (!m) return std::unexpected(m.error());
    return TransferableOutput(m->root());
}

// TransferableOutputList is a run of TransferableOutput records, kTransferableOutputSize bytes each.
class TransferableOutputList {
  public:
    TransferableOutputList() = default;
    explicit TransferableOutputList(zap::List l) : l_(l) {}

    // How many elements the list holds.
    std::int64_t size() const { return l_.size(); }
    // Element i, or the absent TransferableOutput past the end.
    ::xvm::TransferableOutput at(std::int64_t i) const { return ::xvm::TransferableOutput(l_.object(i, kTransferableOutputSize)); }

  private:
    zap::List l_;
};

// TransferableOutputInput collects the field values for NewTransferableOutput. A borrowed field is a view:
// it must outlive the call, not the buffer the call returns.
struct TransferableOutputInput {
    std::uint32_t Offset{};
};

// NewTransferableOutput writes a ZAP-encoded TransferableOutput message into a fresh buffer and returns it.
inline std::vector<std::uint8_t> NewTransferableOutput(const TransferableOutputInput& in) {
    zap::Builder b(256, zap::kVersion2);
    auto ob = b.start_object(kTransferableOutputSize);
    ob.set_u32(kTransferableOutputOffsetOff, in.Offset);
    ob.finish_as_root();
    return b.finish();
}

}  // namespace xvm
