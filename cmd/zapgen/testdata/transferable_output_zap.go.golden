// Code generated by zapgen; DO NOT EDIT.
// source: basetx.zap

package xvm

import (
	zap "github.com/zap-proto/go"
)

const (
	transferableOutputAssetOff  = 0
	transferableOutputOutputOff = 32
	transferableOutputSize      = 40
)

// TransferableOutput is a zero-copy view into a ZAP-encoded TransferableOutput message.
type TransferableOutput struct{ o zap.Object }

// WrapTransferableOutput parses b and returns a typed view. Returns an error if the
// wire-level checks (magic, version, size) fail.
func WrapTransferableOutput(b []byte) (TransferableOutput, error) {
	m, err := zap.Parse(b)
	if err != nil {
		return TransferableOutput{}, err
	}
	return TransferableOutput{o: m.Root()}, nil
}

func (t TransferableOutput) Asset() [32]byte {
	var out [32]byte
	copy(out[:], t.o.BytesFixed(transferableOutputAssetOff, 32))
	return out
}
func (t TransferableOutput) Output() []byte { return t.o.Bytes(transferableOutputOutputOff) }

// TransferableOutputInput collects the field values for NewTransferableOutput.
type TransferableOutputInput struct {
	Asset  [32]byte
	Output []byte
}

// PutTransferableOutput writes a TransferableOutput into b and returns where its object landed.
//
// What a field points AT is written first, in field order, and the fixed
// section last: a pointer always leads backward, to bytes already placed.
func PutTransferableOutput(b *zap.Builder, in TransferableOutputInput) int {
	ob := b.StartObject(transferableOutputSize)
	ob.SetBytesFixed(transferableOutputAssetOff, in.Asset[:])
	ob.SetBytes(transferableOutputOutputOff, in.Output)
	return ob.Finish()
}

// NewTransferableOutput builds a ZAP-encoded TransferableOutput message from in and returns the bytes.
func NewTransferableOutput(in TransferableOutputInput) []byte {
	b := zap.NewBuilderV2(256)
	b.SetRoot(PutTransferableOutput(b, in))
	return b.Finish()
}
