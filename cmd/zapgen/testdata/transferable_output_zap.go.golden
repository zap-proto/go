// Code generated by zapgen; DO NOT EDIT.
// source: basetx.zap

package xvm

import (
	zap "github.com/zap-proto/go"
)

const (
	transferableOutputOffsetOff = 0
	transferableOutputSize      = 4
)

// TransferableOutput is a zero-copy view into a ZAP-encoded TransferableOutput message.
type TransferableOutput struct{ o zap.Object }

// WrapTransferableOutput parses b and returns a typed view. Returns an error if the
// wire-level checks (magic, version, size) fail.
func WrapTransferableOutput(b []byte) (TransferableOutput, error) {
	m, err := zap.Parse(b)
	if err != nil {
		return TransferableOutput{}, err
	}
	return TransferableOutput{o: m.Root()}, nil
}

func (t TransferableOutput) Offset() uint32 { return t.o.Uint32(transferableOutputOffsetOff) }

// Record is the transferableOutputSize bytes this TransferableOutput occupies where it lies.
func (t TransferableOutput) Record() []byte { return t.o.BytesFixed(0, transferableOutputSize) }

// TransferableOutputList is a run of TransferableOutput records, transferableOutputSize bytes each.
type TransferableOutputList struct{ l zap.List }

// Len is how many elements the list holds.
func (x TransferableOutputList) Len() int { return x.l.Len() }

// At is element i, or the absent TransferableOutput past the end.
func (x TransferableOutputList) At(i int) TransferableOutput {
	return TransferableOutput{o: x.l.Object(i, transferableOutputSize)}
}

// TransferableOutputInput collects the field values for NewTransferableOutput.
type TransferableOutputInput struct {
	Offset uint32
}

// NewTransferableOutput builds a ZAP-encoded TransferableOutput message from in and returns the bytes.
func NewTransferableOutput(in TransferableOutputInput) []byte {
	b := zap.NewBuilderV2(256)
	ob := b.StartObject(transferableOutputSize)
	ob.SetUint32(transferableOutputOffsetOff, in.Offset)
	ob.FinishAsRoot()
	return b.Finish()
}
