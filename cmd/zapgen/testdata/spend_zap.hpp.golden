// Code generated by zapgen; DO NOT EDIT.
// source: basetx.zap
// SPDX-License-Identifier: BSD-3-Clause-Eco

#pragma once

#include <array>
#include <cstdint>
#include <cstring>
#include <expected>
#include <span>
#include <vector>

#include <zap/zap.hpp>

namespace xvm {

class Spend;

// Field byte offsets for Spend, and the size of its fixed section.
inline constexpr std::int64_t kSpendTxIDOff = 0;
inline constexpr std::int64_t kSpendOutputIndexOff = 32;
inline constexpr std::int64_t kSpendAssetIDOff = 36;
inline constexpr std::int64_t kSpendInputOff = 68;
inline constexpr std::int64_t kSpendSize = 76;

// Spend is a zero-copy view into a ZAP-encoded Spend message. It borrows the
// bytes it was built over, which must outlive it, and copies no field.
class Spend {
  public:
    Spend() = default;
    explicit Spend(zap::Object o) : o_(o) {}

    bool is_null() const { return o_.is_null(); }
    zap::Object object() const { return o_; }

    std::span<const std::uint8_t> TxID() const {
        static constexpr std::array<std::uint8_t, 32> zero{};
        const auto b = o_.bytes_fixed(kSpendTxIDOff, 32);
        return b.size() == 32 ? b : std::span<const std::uint8_t>(zero);
    }
    std::uint32_t OutputIndex() const { return o_.u32(kSpendOutputIndexOff); }
    std::span<const std::uint8_t> AssetID() const {
        static constexpr std::array<std::uint8_t, 32> zero{};
        const auto b = o_.bytes_fixed(kSpendAssetIDOff, 32);
        return b.size() == 32 ? b : std::span<const std::uint8_t>(zero);
    }
    std::span<const std::uint8_t> Input() const { return o_.bytes(kSpendInputOff); }

  private:
    zap::Object o_;
};

// WrapSpend parses b and returns a typed view over it, or says why b is not a
// message. The view borrows b.
inline std::expected<Spend, zap::Error> WrapSpend(std::span<const std::uint8_t> b) {
    auto m = zap::Message::parse(b);
    if (!m) return std::unexpected(m.error());
    return Spend(m->root());
}

// The value records, declared before they are defined.
struct SpendInput;

// SpendInput collects the field values for NewSpend. A borrowed field is a view:
// it must outlive the call, not the buffer the call returns.
struct SpendInput {
    std::array<std::uint8_t, 32> TxID{};
    std::uint32_t OutputIndex{};
    std::array<std::uint8_t, 32> AssetID{};
    std::span<const std::uint8_t> Input{};
};

// The write side, declared before it is defined so a struct may point at
// one that comes later in the schema.
inline std::vector<std::uint8_t> NewSpend(const SpendInput& in);
inline std::int64_t AppendSpend(zap::Builder& b, const SpendInput& in);

// AppendSpend writes one Spend object into b — every payload it points at first,
// then the object itself — and returns the object's offset.
inline std::int64_t AppendSpend(zap::Builder& b, const SpendInput& in) {
    auto ob = b.start_object(kSpendSize);
    ob.set_bytes_fixed(kSpendTxIDOff, std::span<const std::uint8_t>(in.TxID));
    ob.set_u32(kSpendOutputIndexOff, in.OutputIndex);
    ob.set_bytes_fixed(kSpendAssetIDOff, std::span<const std::uint8_t>(in.AssetID));
    ob.set_bytes(kSpendInputOff, in.Input);
    return ob.finish();
}

// NewSpend writes a ZAP-encoded Spend message into a fresh buffer and returns it.
inline std::vector<std::uint8_t> NewSpend(const SpendInput& in) {
    zap::Builder b(256, zap::kVersion2);
    auto ob = b.start_object(kSpendSize);
    ob.set_bytes_fixed(kSpendTxIDOff, std::span<const std::uint8_t>(in.TxID));
    ob.set_u32(kSpendOutputIndexOff, in.OutputIndex);
    ob.set_bytes_fixed(kSpendAssetIDOff, std::span<const std::uint8_t>(in.AssetID));
    ob.set_bytes(kSpendInputOff, in.Input);
    ob.finish_as_root();
    return b.finish();
}

}  // namespace xvm
